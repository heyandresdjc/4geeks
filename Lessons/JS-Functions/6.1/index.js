// starting value
let dude = 'abacddbec'
// turn starting value to a list of strings
function nonRepeated(str) {
    let str_list = str.split("");
    str_list.forEach(element => {
        
    });
}

console.log(found);